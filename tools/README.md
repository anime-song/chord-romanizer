# Development tools

- `generate_python_golden.py`: Python 0.1.9の互換golden fixtureを再生成する
- `reproduce_issue.py`: 旧issue進行をPython実装で手動確認する
- `publish.ps1`: Python packageをbuild・検査し、確認後PyPIへ公開する

各コマンドはリポジトリルートをcurrent directoryとして実行してください。`reproduce_issue.py`だけは任意のcurrent directoryから実行できます。
